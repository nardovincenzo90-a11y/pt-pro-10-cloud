(() => {'use strict';const A=window.PTAPP,{state,esc,fmt,shell,readiness,smartAdvice,go}=A;
A.register('home',async()=>{state.view='home';const b=state.boot,p=b.profile||{},plan=b.plan,days=b.days||[],sessions=b.sessions||[],m=b.measures||[],now=Date.now(),week=sessions.filter(s=>now-new Date(s.started_at).getTime()<7*864e5),mins=week.reduce((a,s)=>a+Number(s.duration_minutes||0),0),last=m[0],prev=m[1],next=days[0],r=readiness(),ad=smartAdvice(r),vol=week.reduce((a,s)=>a+Number(s.total_volume||0),0),streak=week.length,trend=last?.weight_kg&&prev?.weight_kg?Number(last.weight_kg)-Number(prev.weight_kg):null,nextEvent=(b.events||[]).find(e=>new Date(e.starts_at)>new Date());shell(`<section class="hero"><div class="card heroMain"><div class="eyebrow">${new Date().toLocaleDateString('it-IT',{weekday:'long',day:'numeric',month:'long'})}</div><h1>Ciao ${esc(p.display_name||p.first_name||'Vincenzo')}.</h1><p class="muted">${plan?`Scheda attiva: <b>${esc(plan.title)}</b>`:'Nessuna scheda attiva'}</p>${next?`<button class="btn primary full" id="startToday">▶ Inizia ${esc(next.name)}</button>`:''}</div><div class="kpis"><div class="kpi"><b>${week.length}</b><span>Allenamenti settimana</span></div><div class="kpi"><b>${last?.weight_kg?fmt(last.weight_kg)+' kg':'—'}</b><span>Peso</span></div><div class="kpi"><b>${r}%</b><span>Prontezza</span></div><div class="kpi"><b>${mins}′</b><span>Minuti settimana</span></div></div></section><section class="section"><div class="sectionHead"><h2>Smart Coach</h2><button class="linkBtn" data-route="coach-smart">Analisi completa</button></div><div class="coach"><div class="coachScore">${r}</div><div><b>${ad[0]}</b><p>${ad[1]}</p></div></div><div class="insightGrid"><div class="miniCard"><b>${streak}</b><span>sedute ultimi 7 giorni</span></div><div class="miniCard"><b>${trend===null?'—':(trend>=0?'+':'')+fmt(trend)+' kg'}</b><span>trend peso</span></div><div class="miniCard"><b>${fmt(vol,0)}</b><span>volume registrato</span></div><div class="miniCard"><b>${(b.goals||[]).filter(g=>g.status==='active').length}</b><span>obiettivi attivi</span></div></div></section>${nextEvent?`<section class="section"><div class="sectionHead"><h2>Prossimo evento</h2></div><button class="dayCard" data-route="calendar"><div><b>${esc(nextEvent.title)}</b><span>${new Date(nextEvent.starts_at).toLocaleString('it-IT')}</span></div></button></section>`:''}<section class="section"><div class="sectionHead"><h2>Allenamenti</h2><span class="muted">${days.length} giorni</span></div><div class="dayGrid">${days.map(d=>`<button class="dayCard" data-day="${d.id}"><div><b>${esc(d.name)}</b><span>${esc(d.focus||'')}</span></div></button>`).join('')||'<div class="card empty">Nessun giorno programmato.</div>'}</div></section>`);document.querySelectorAll('[data-day]').forEach(x=>x.onclick=()=>go('workout-day',x.dataset.day));if(next)document.getElementById('startToday').onclick=()=>go('workout-day',next.id);A.bindRoutes()});})();